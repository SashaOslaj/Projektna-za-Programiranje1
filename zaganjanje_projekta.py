from Projekt import *

#download = download_url_to_string(stadiums_url)

#text = save_string_to_file(download, stadiums_directory, frontpage_filename)

#result = get_url(stadiums_url)
#result.encoding

#reade_page = read_file_to_string(stadiums_directory, frontpage_filename)


page = page_to_table(read_file_to_string(stadiums_directory, frontpage_filename))


